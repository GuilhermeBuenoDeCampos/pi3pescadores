{
  "name": "pi3pescadores",
  "version": "1.0.0",
  "description": "Projeto PI3 Pescadores - Frontend e Backend",
  "scripts": {
    "dev": "npx concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend start",
    "dev:frontend": "npm --prefix frontend run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@api/melhorenvio": "file:.api/apis/melhorenvio",
    "cors": "^2.8.6"
  }
}
