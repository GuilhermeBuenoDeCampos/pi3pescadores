# migrations

Contém as migrations do banco de dados.

Cada migration descreve mudanças de estrutura (criação/alteração de tabelas, índices e constraints).

Neste projeto, as migrations seguem o formato do Sequelize CLI.

O arquivo `.sequelizerc` aponta o CLI para esta pasta.
